#!/bin/sh
set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

if ! command -v uv >/dev/null 2>&1; then
    echo "uv not found. Running bootstrap..."
    "$SCRIPT_DIR/bootstrap.sh"
    export PATH="$HOME/.local/bin:$PATH"
fi

cd "$SCRIPT_DIR"
exec uv run python -m lxs "$@"
